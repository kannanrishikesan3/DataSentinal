"""Versioned API routers (`/api/v1/...`). Import `api_v1_router` from
`datasentinel_backend.api.v1.router` directly (not re-exported here) to
avoid a circular import through this package's own `__init__.py`.
"""
